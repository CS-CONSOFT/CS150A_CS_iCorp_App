import { View } from "react-native";

const Separator = () => {
    return (
        <View
            style={{
                borderBottomColor: '#C3C3C3',
                borderBottomWidth: 1,
            }}
        />
    );
}

export default Separator;